// let nombre = "Natasha Dianeeey";

// saludar(nombre);

// function nombre(parametroFuncion) {
//     console.log('Hola ' + parametroFuncion); 
// }



module.exports = {
    // Funcion anonima, por que no tiene nombre
    suma: function (a, b) {
        return a + b;
    },
    resta: function (a, b) {
        return a - b;
    },
    multiplicacion: function (a, b) {
        return a * b;
    },
    division: function (a, b) {
        return a / b;
    }
}

// object.propiedad
// // Cuando tenemos un valor es propiedad
// // Cuando tenemos una propiedad es un metodo

// module.exports 